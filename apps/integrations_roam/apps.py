from django.apps import AppConfig


class IntegrationsRoamConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integrations_roam"
    label = "integrations_roam"
